<html>
<body>
  <div id="product_list" >

<h1>Listing addresses</h1>

<table id="table">
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Phone</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @addresses.each do |address| %>
  <tr>
    <td ><%= address.name %></td>
    <td><%= address.email %></td>
    <td><%= address.phone %></td>
    <td class="list_action"><%= link_to 'Show', address %><br/>
    <%= link_to 'Edit', edit_address_path(address) %><br/>
   <%= link_to 'Destroy', address, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Address', new_address_path %>
</div>
</body>
</html>